import { WholeDayRangeDirective } from './whole-day-range.directive';

describe('WholeDayRangeDirective', () => {
  it('should create an instance', () => {
    const directive = new WholeDayRangeDirective();
    expect(directive).toBeTruthy();
  });
});
