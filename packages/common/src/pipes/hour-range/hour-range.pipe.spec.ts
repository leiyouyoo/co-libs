import { HourRangePipe } from './hour-range.pipe';

describe('HourRangePipe', () => {
  it('create an instance', () => {
    const pipe = new HourRangePipe();
    expect(pipe).toBeTruthy();
  });
});
