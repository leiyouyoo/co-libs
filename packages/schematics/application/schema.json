{
  "$schema": "http://json-schema.org/schema",
  "id": "SchematicsAngularApplication",
  "title": "Schematics Angular Application",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the application."
    },
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      }
    }
  },
  "required": [
    "name"
  ]
}